# 工具导航网站

一个基于 Next.js + Tailwind CSS + MySQL 的现代化工具导航网站，包含前台展示和后台管理功能。

## 功能特性

### 前台功能

- 🎨 现代化的响应式设计
- 🔍 实时搜索功能
- 📁 分类筛选
- ⭐ 推荐工具展示
- 📱 移动端适配

### 后台管理

- 🔐 安全的登录认证
- 🛠️ 工具管理（增删改查）
- 📂 分类管理（增删改查）
- 📊 数据统计仪表板

## 技术栈

- **框架**: Next.js 14+ (App Router)
- **样式**: Tailwind CSS 4
- **数据库**: MySQL 8+
- **ORM**: Prisma
- **认证**: NextAuth.js
- **语言**: TypeScript

## 快速开始

### 1. 安装依赖

```bash
cd tool-navigation
npm install
```

### 2. 配置环境变量

编辑 `.env.local` 文件，配置数据库连接：

```env
DATABASE_URL="mysql://用户名:密码@localhost:3306/数据库名"
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"
```

### 3. 初始化数据库

```bash
# 推送数据库结构
npm run db:push

# 初始化示例数据（包括管理员账号）
npm run db:seed
```

### 4. 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看网站。

## 默认管理员账号

初始化数据库后，可以使用以下账号登录后台：

- **用户名**: `admin`
- **密码**: `admin123`
- **后台地址**: [http://localhost:3000/admin/login](http://localhost:3000/admin/login)

⚠️ **重要**: 首次登录后请立即修改密码！

## 项目结构

```
tool-navigation/
├── app/                      # Next.js App Router
│   ├── (frontend)/          # 前台页面
│   │   └── page.tsx         # 首页
│   ├── admin/               # 后台管理
│   │   ├── login/           # 登录页
│   │   ├── dashboard/       # 仪表板
│   │   ├── tools/           # 工具管理
│   │   └── categories/      # 分类管理
│   ├── api/                 # API 路由
│   │   ├── auth/            # 认证
│   │   ├── tools/           # 工具 API
│   │   └── categories/      # 分类 API
│   ├── layout.tsx           # 根布局
│   └── globals.css          # 全局样式
├── components/              # React 组件
│   ├── ToolCard.tsx         # 工具卡片
│   ├── CategoryFilter.tsx   # 分类筛选
│   ├── AdminLayout.tsx      # 后台布局
│   └── Providers.tsx        # SessionProvider
├── lib/                     # 工具函数
│   ├── prisma.ts            # Prisma 客户端
│   └── auth.ts              # NextAuth 配置
├── prisma/                  # 数据库
│   ├── schema.prisma        # 数据库模型
│   └── seed.ts              # 初始化数据
└── types/                   # TypeScript 类型定义
    └── next-auth.d.ts       # NextAuth 类型扩展
```

## 数据库模型

### Category (分类)

- `id`: 主键
- `name`: 分类名称
- `slug`: URL 标识符
- `description`: 分类描述

### Tool (工具)

- `id`: 主键
- `name`: 工具名称
- `url`: 工具地址
- `description`: 工具介绍
- `icon`: 图标 URL
- `categoryId`: 所属分类
- `featured`: 是否推荐

### User (用户)

- `id`: 主键
- `username`: 用户名
- `password`: 加密密码
- `email`: 邮箱
- `role`: 角色

## 可用命令

```bash
# 开发
npm run dev          # 启动开发服务器
npm run build        # 构建生产版本
npm run start        # 启动生产服务器

# 数据库
npm run db:push      # 推送数据库结构
npm run db:seed      # 初始化示例数据
npm run db:studio    # 打开 Prisma Studio

# 代码质量
npm run lint         # 运行 ESLint
```

## API 端点

### 工具 API

- `GET /api/tools` - 获取工具列表（支持分页、搜索、筛选）
- `POST /api/tools` - 创建工具（需要认证）
- `GET /api/tools/[id]` - 获取单个工具
- `PUT /api/tools/[id]` - 更新工具（需要认证）
- `DELETE /api/tools/[id]` - 删除工具（需要认证）

### 分类 API

- `GET /api/categories` - 获取所有分类
- `POST /api/categories` - 创建分类（需要认证）
- `PUT /api/categories/[id]` - 更新分类（需要认证）
- `DELETE /api/categories/[id]` - 删除分类（需要认证）

## 部署

### Vercel 部署

1. 推送代码到 GitHub
2. 在 Vercel 导入项目
3. 配置环境变量
4. 部署

### 数据库配置

推荐使用以下 MySQL 云服务：

- PlanetScale
- Railway
- AWS RDS
- 阿里云 RDS

## 常见问题

### 1. 数据库连接失败

确保 MySQL 服务正在运行，并且 `.env.local` 中的连接字符串正确。

### 2. 登录失败

确保已运行 `npm run db:seed` 初始化管理员账号。

### 3. 样式不生效

清除 `.next` 缓存：

```bash
rm -rf .next
npm run dev
```

## 许可证

MIT

## 作者

Created with ❤️ by Antigravity AI
